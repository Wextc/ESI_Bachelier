# Les tuples - ()

Un tuple est une structure de données en Python qui permet de regrouper plusieurs valeurs sous un même nom. Chaque valeur peut être consultée à l’aide d’un indice, c’est-à-dire un numéro qui indique sa position dans le tuple. Comme pour les listes, l’indice du premier élément est toujours 0. La particularité du tuple est qu’il est immuable : une fois créé, son contenu ne peut plus être modifié.

Pour créer un tuple, on peut utiliser des parenthèses, comme dans (1, 2, 3, 4, 5), ou bien la fonction tuple(), par exemple tuple(range(10_000_000)). Dans le premier cas, le tuple contient les valeurs 1 à 5, accessibles directement par leur indice : tuple_1[0] vaut 1, tuple_1[3] vaut 4, etc. Dans le second cas, le tuple contient une suite de valeurs allant de 0 à 9 999 999, et l’accès se fait de la même manière : tuple_2[1_000_000] renvoie 1 000 000.

Il est également possible d’écrire des nombres de grande taille en séparant les chiffres avec des caractères underscore (\_) afin de rendre le code plus lisible. Par exemple, 10_000_000 est strictement équivalent à 10000000. Python ignore ces soulignements lors de l’interprétation du nombre, mais l’écriture est plus claire pour l’humain.

Exemples :

```
tuple_1 = (1, 2, 3, 4, 5)

tuple_2 = tuple(range(10_000_000))

```

<b>Voici toutes les méthodes qui s'applique aux tuples.</b>

<b> Méthodes de tuple (Python)</b>

🔹 Méthodes d’instance

| Méthode                  | Description                         |
| ------------------------ | ----------------------------------- |
| `__add__(value)`         | Retourne `self + value`             |
| `__contains__(key)`      | Vérifie si `key` est dans le tuple  |
| `__eq__(value)`          | `self == value`                     |
| `__ge__(value)`          | `self >= value`                     |
| `__getattribute__(name)` | Récupère un attribut                |
| `__getitem__(key)`       | Accès par index ou slice            |
| `__getnewargs__()`       | Utilisé en interne pour le pickling |
| `__gt__(value)`          | `self > value`                      |
| `__hash__()`             | Retourne le hash du tuple           |
| `__iter__()`             | Renvoie un itérateur sur le tuple   |
| `__le__(value)`          | `self <= value`                     |
| `__len__()`              | Longueur du tuple                   |
| `__lt__(value)`          | `self < value`                      |
| `__mul__(value)`         | `self * value` (répétition)         |
| `__ne__(value)`          | `self != value`                     |
| `__repr__()`             | Représentation officielle du tuple  |
| `__rmul__(value)`        | `value * self`                      |

<b>Méthodes de classe</b>

| Méthode                  | Description                                 |
| ------------------------ | ------------------------------------------- |
| `__class_getitem__(...)` | Supporte les annotations de type (PEP 585). |

<b>Méthodes statiques</b>

| Sous-classe          |
| -------------------- |
| `asyncgen_hooks`     |
| `UnraisableHookArgs` |

<b>Exercice 1 : Maximum</b>

Soit le tuple suivant : (-4, 12, -71, 33, 20, 32, 96, -22, -7, 70, 82, 62, 11, 72, -36, -16, 84) Écrivez une fonction ya_max qui recherche et retourne la valeur maximale de ce tuple. Affichez le résultat de votre fonction de recherche de maximum et celui de la fonction built in max.

```
def ya_max(t):
    max_val = t[0]              # on part du premier élément
    for val in t:               # on parcourt le tuple
        if val > max_val:       # si on trouve plus grand
            max_val = val
    return max_val


# Tuple donné
t = (-4, 12, -71, 33, 20, 32, 96, -22, -7, 70, 82, 62, 11, 72, -36, -16, 84)

# Résultats
print("Maximum trouvé avec ya_max :", ya_max(t))
print("Maximum trouvé avec max()  :", max(t))

```

#### Temps d’accès

Le temps d’accès correspond au temps nécessaire pour qu’un ordinateur atteigne une zone mémoire et en récupère la valeur qui s’y trouve. Pour mesurer ce temps, on enregistre simplement l’instant précis juste avant l’accès et l’instant juste après. La différence entre ces deux instants représente alors la durée réelle de l’opération.

Dans l’exemple donné, on utilise le module time pour mesurer ce délai. La fonction access_time prend un tuple, relève l’heure courante, parcourt tous ses éléments un à un, puis relève de nouveau l’heure. En soustrayant les deux mesures, on obtient le temps total nécessaire pour accéder à l’ensemble des valeurs du tuple. Lorsque le tuple contient très peu d’éléments, ce temps est presque imperceptible. Mais plus le tuple devient grand, plus la durée totale de lecture de tous ses éléments augmente et peut finir par devenir significative.

![permission_cat_personnes](https://github.com/Wextc/ESI_Bachelier/blob/main/DEV1/Laboratoire/TD10/img/temps_acces.png)

#### Immuabilité

Les tuples sont immuables : on ne peut ni changer leurs éléments ni modifier leur taille.
Pour créer de nouveaux tuples, on utilise plutôt des opérations comme + ou \*, qui génèrent un nouvel objet sans affecter les anciens.

Un tuple en Python est une structure de données immuable : une fois créé, son contenu ne peut pas être modifié. Cela signifie que :

On ne peut pas changer l’une de ses valeurs.

On ne peut pas ajouter un élément.

On ne peut pas supprimer un élément.

Par exemple :

```
tuple_1 = (10, 20, 30)
tuple_1[0] = -1   # Erreur, un tuple ne peut pas être modifié

```

Comme les tuples sont immuables, Python ne propose aucune méthode pour ajouter ou retirer un élément (contrairement aux listes).

Concaténation avec l’opérateur +

Même si on ne peut pas modifier un tuple, on peut en créer de nouveaux à partir d’autres tuples.
L’opérateur + permet de concaténer deux tuples, c’est-à-dire de former un nouveau tuple contenant toutes les valeurs des deux :

```
t1 = (1, 2)
t2 = (3, 4)
t3 = t1 + t2
print(t3)  # (1, 2, 3, 4)

```

Ici, ni t1 ni t2 ne changent : c’est un nouveau tuple qui est créé.

Répétition avec l’opérateur \*

L’opérateur \*, appliqué à un tuple et un entier n, crée un nouveau tuple où les valeurs du tuple d’origine sont répétées n fois :

```
t = (5, 6)
t4 = t * 3
print(t4)  # (5, 6, 5, 6, 5, 6)

```

Là encore, le tuple initial n’est pas modifié : un nouveau tuple est produit.

#### Découpage

Le découpage (ou slicing) est une façon très pratique d’extraire une partie d’un tuple en Python.
On utilise la notation a[i:j] pour récupérer tous les éléments dont l’indice est entre i (inclus) et j (exclu). C’est ce qu’on appelle un intervalle semi-ouvert :

i est inclus → l’élément d’indice i fait partie du résultat

j est exclu → l’élément d’indice j ne fait pas partie du résultat

Le slicing ne modifie pas le tuple d’origine : il crée un nouveau tuple, dont les indices repartent à 0.

Exemple simple

```
tuple_1 = ('a', 'b', 'c', 'd', 'e')
tuple_3 = tuple_1[2:4]
print(tuple_3)

```

Résultat :

```
('c', 'd')

```

Explication :

```
tuple_1[2] → 'c' (inclus)

tuple_1[3] → 'd' (inclus)

tuple_1[4] → 'e' (exclu car c’est la borne j)

```

Donc seuls 'c' et 'd' apparaissent dans le nouveau tuple.

Autres exemples utiles

1. Extraire depuis un indice jusqu’à la fin

```
t = (10, 20, 30, 40)
print(t[1:])      # (20, 30, 40)

```

2. Extraire depuis le début jusqu’à un indice

```
t = (10, 20, 30, 40)
print(t[:3])      # (10, 20, 30)

```

3. Extraire tout sauf le premier et le dernier élément

```
t = (1, 2, 3, 4, 5)
print(t[1:-1])    # (2, 3, 4)

```

4. Copie complète du tuple

```
t = (7, 8, 9)
print(t[:])       # (7, 8, 9)

```

<b>Les différentes variables de tuples</b>

Soit un tuple x.

```
x = (11, 12, 13, 14, 15, 16, 17)

```

<b>Variantes simples (borne gauche, borne droite)</b>

| Expression | Description                           | Résultat                       |
| ---------- | ------------------------------------- | ------------------------------ |
| `x[:3]`    | Du début jusqu’à l’indice 3 (exclu)   | `(11, 12, 13)`                 |
| `x[3:]`    | À partir de l’indice 3 jusqu’à la fin | `(14, 15, 16, 17)`             |
| `x[:]`     | Copie complète du tuple               | `(11, 12, 13, 14, 15, 16, 17)` |
| `x[2:5]`   | De l’indice 2 à 5 (exclu)             | `(13, 14, 15)`                 |

<b>Variantes avec step positif</b>

| Expression | Description                       | Résultat           |
| ---------- | --------------------------------- | ------------------ |
| `x[0:7:2]` | Un élément sur deux entre 0 et 7  | `(11, 13, 15, 17)` |
| `x[1:7:2]` | Un élément sur deux à partir de 1 | `(12, 14, 16)`     |
| `x[::2]`   | Toute la séquence, pas de 2       | `(11, 13, 15, 17)` |

Step = 3

| Expression | Description            | Résultat       |
| ---------- | ---------------------- | -------------- |
| `x[0:7:3]` | Un élément sur trois   | `(11, 14, 17)` |
| `x[1:6:3]` | Pas de 3 à partir de 1 | `(12, 15)`     |

<b>Step positif mais bornes larges</b>

| Expression | Description                           | Résultat                       |
| ---------- | ------------------------------------- | ------------------------------ |
| `x[0:100]` | Stop trop grand → s’arrête au maximum | `(11, 12, 13, 14, 15, 16, 17)` |
| `x[::5]`   | Saut de 5                             | `(11, 16)`                     |

<b>Variantes avec step négatif (lecture à l’envers)</b>

| Expression | Description   | Résultat                       |
| ---------- | ------------- | ------------------------------ |
| `x[::-1]`  | Tuple inversé | `(17, 16, 15, 14, 13, 12, 11)` |

Step négatif simple

| Expression  | Description                | Résultat               |
| ----------- | -------------------------- | ---------------------- |
| `x[5:2:-1]` | De 5 à 2 exclu, en arrière | `(16, 15, 14)`         |
| `x[6:1:-1]` | De 6 à 1 exclu             | `(17, 16, 15, 14, 13)` |
| `x[4:0:-1]` | De 4 à 0 exclu             | `(15, 14, 13, 12)`     |

<b>Step négatif avec step > 1</b>

Step = –2

| Expression  | Description             | Résultat           |
| ----------- | ----------------------- | ------------------ |
| `x[6:0:-2]` | Recule de 2             | `(17, 15, 13, 11)` |
| `x[5:1:-2]` | Recule de deux depuis 5 | `(16, 14, 12)`     |

Step = –3

| Expression  | Description             | Résultat       |
| ----------- | ----------------------- | -------------- |
| `x[6:0:-3]` | Recule de 3             | `(17, 14, 11)` |
| `x[5:2:-3]` | Recule de 3 jusqu’à > 2 | `(16,)`        |
| `x[5:3:-3]` | Recule de 3 jusqu’à > 3 | `(16,)`        |

<b>Cas où le slicing donne un tuple VIDE</b>

| Expression  | Raison                         | Résultat |
| ----------- | ------------------------------ | -------- |
| `x[5:2:1]`  | Step positif mais start > stop | `()`     |
| `x[4:1:1]`  | Step positif mais start > stop | `()`     |
| `x[1:5:-1]` | Step négatif mais start < stop | `()`     |
| `x[1:2:-1]` | Atteint la borne stop (exclue) | `()`     |

#### La mutuabilité

En Python, les listes et les tuples diffèrent principalement par leur mutabilité, c’est-à-dire leur capacité à être modifiés après leur création.
Les listes sont des objets mutables : on peut changer leur contenu à tout moment. Cela inclut la modification d’une valeur existante, l’ajout de nouveaux éléments, la suppression d’autres, ou encore la réorganisation de l’ensemble. Par exemple, si l’on crée la liste liste = [1, 2, 3], il est possible de remplacer son premier élément par 100, d’y ajouter un 4 grâce à append, ou de supprimer l’élément d’indice 1. Toutes ces opérations modifient réellement la structure stockée en mémoire.

À l’inverse, les tuples sont immutables : une fois créés, ils ne peuvent plus être modifiés. Il est impossible de changer une valeur, d’en ajouter ou d’en supprimer, et aucune méthode telle que append, remove ou pop n’existe pour eux. Ainsi, si l’on écrit t = (1, 2, 3), tenter de faire t[0] = 100 provoquera une erreur, car Python interdit toute modification directe du contenu d’un tuple. Celui-ci reste donc définitivement figé.

Cependant, certaines opérations peuvent donner l’impression qu’un tuple change. Par exemple, lorsqu’on écrit :

```
t = (1, 2, 3)
t = t + (4,)

```

le tuple ne se modifie pas réellement. Python crée en réalité un nouveau tuple, (1, 2, 3, 4), puis affecte cette nouvelle structure à la variable t. L’ancien tuple reste inchangé ; il est simplement remplacé dans la variable. Ainsi, même si la syntaxe peut sembler similaire à une modification, la nature immuable des tuples reste intacte.

| Type      | Mutable ? | Peut changer sa taille ? | Peut changer ses valeurs ? | Méthodes d’ajout/suppression ? |
| --------- | --------- | ------------------------ | -------------------------- | ------------------------------ |
| **Liste** | ✔ Oui     | ✔ Oui                    | ✔ Oui                      | ✔ Oui (append, pop, remove…)   |
| **Tuple** | ❌ Non    | ❌ Non                   | ❌ Non                     | ❌ Non                         |
